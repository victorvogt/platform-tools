[gitea_source]
# Current Gitea instance — used only for vault var context (S3 creds come from its vault.yml)
gitea-source ansible_host=51.15.141.170 ansible_user=root

[gitea_target]
# Replace <TARGET_IP> with your destination server's IP address
gitea-target ansible_host=<TARGET_IP> ansible_user=root
